# Code review — src/

Nálezy zoradené podľa toho, ako boli objavené (adresár po adresári). Formát: **súbor:riadok** — čo je zle a prečo.

## src/Analyzer

- [OPRAVENÉ] **GdImageAnalyzer.php:48-52** — delenie nulou pri `$height === 0`. Vetva `else` počíta `$width * (64 / $height)`; GD vie pri poškodenom/degenerovanom vstupe cez `imagecreatefromstring` vrátiť obrázok s `imagesy() === 0`, čo tu spadne na `DivisionByZeroError` namiesto riadenej výnimky. *(Fix: `calculateTargetDimensions()` teraz explicitne validuje `$width`/`$height >= 1` a vyhadzuje `ImageProcessingException` namiesto delenia nulou; pokryté testami `testCalculateTargetDimensionsThrowsForZero{Width,Height}`.)*
- [OPRAVENÉ] **GdImageAnalyzer.php:36** — `@imagecreatefromstring($data)` potláča chybu operátorom `@`; pri zlyhaní sa stratí skutočný dôvod (warning z GD) a vyhodí sa len generická správa, čo sťažuje diagnostiku poškodených uploadov. *(Fix: dočasný `set_error_handler()` zachytí skutočný GD warning a pripojí ho do správy `ImageProcessingException`; pokryté testom `testAnalyzeThrowsWithDiagnosticGdMessageForCorruptData`.)*
- [OPRAVENÉ, bez zmeny] **GdImageAnalyzer.php:54-68** — GD zdroje `$image` a `$resizedImage` sa nikdy neuvoľňujú cez `imagedestroy()`. Pri dávkovom spracovaní (CLI príkaz, worker) sa pamäť drží dlhšie než treba, kým ju neupratú GC. *(Overené: od PHP 8.0 sú GD obrázky bežné refcountované objekty uvoľňované okamžite pri zániku poslednej referencie (funkcia scope končí pri `return`); `imagedestroy()` je od PHP 8.5 formálne deprecated presne s dôvodom „has no effect since PHP 8.0" — pridanie volania by len generovalo deprecation warningy bez reálneho prínosu. Kód ponechaný bez zmeny.)*
- [OPRAVENÉ] **GdImageAnalyzer.php:61-64** — `imagecolorat` na truecolor obrázku úplne ignoruje alfa kanál. Priehľadné oblasti PNG sa započítajú svojou RGB hodnotou (často čierna) namiesto blendu, takže blurhash priehľadných obrázkov bude mať tmavé okraje/halo namiesto neutrálneho výsledku. *(Fix: `$resizedImage` teraz zachováva alfa kanál cez `imagealphablending(false)`+`imagesavealpha(true)` pred `imagecopyresampled`, a nová `blendAlpha()` blenduje poloprieshľadné pixely na neutrálne biele pozadie namiesto čierneho halo efektu; pokryté testami `testBlendAlpha*` a `testAnalyzePreservesTransparencyThroughResizeInsteadOfBlackHalo`.)*
- [OPRAVENÉ] **ImagickAnalyzer.php:55** — chytá sa iba `\ImagickException`. `readImageFile`, `thumbnailImage`, `getImageWidth/Height` a `exportImagePixels` môžu vyhodiť aj iné výnimky (napr. `\ImagickPixelException`, TypeError pri zlom argumente), ktoré prejdú nezachytené a poruší sa kontrakt „wrapni chyby do ImageProcessingException" deklarovaný v `ImageAnalyzerInterface`. *(Fix: `catch (\ImagickException $e)` nahradené `catch (\Throwable $e)`; pokryté testom `testAnalyzeWrapsNonImagickThrowablesIntoImageProcessingException`, ktorý reprodukuje reálny `\TypeError` zo zavretého stream resourcu.)*
- [OPRAVENÉ] **ImagickAnalyzer.php** — `Imagick` objekt sa nikdy neuvoľňuje (`->clear()`/`->destroy()`), rovnaký problém životného cyklu ako pri GD analyzátore — natívna pamäť sa hromadí naprieč volaniami v rámci jedného procesu (relevantné pre dlhobežiaci Messenger worker). *(Fix: `finally { $image->clear(); }` pridané okolo spracovania. Poznámka: na rozdiel od GD, Imagick drží natívnu C-knižnicu ImageMagick mimo PHP refcountingu, takže `clear()` má tu reálny efekt — nie je to no-op ako pri GD; overené existujúcimi `analyze()` testami, samostatný test na uvoľnenie pamäte nebol prakticky realizovateľný bez flaky memory-usage assertions.)*
- [OPRAVENÉ] **ImagickAnalyzer.php:40-54** — žiadna validácia `$width`/`$height` pred cyklom `exportImagePixels`/skladaním `$formattedPixels`; degenerovaný vstup (0 alebo extrémne úzky obrázok) prejde bez kontroly do `Blurhash::encode()`. *(Fix: nová `assertValidDimensions()` volaná pred `exportImagePixels`, vyhadzuje `ImageProcessingException` pre `$width < 1 || $height < 1`; pokryté testami `testAssertValidDimensionsThrowsForZero{Width,Height}`.)*

## src/Loader

- [OPRAVENÉ] **FileSystemLoader.php:29** — `load()` pri každom volaní prepíše `$this->file` novým resource bez zatvorenia predchádzajúceho handlu. Pri opakovanom použití tej istej inštancie v rámci jedného procesu (napr. dlhobežiaci Messenger worker spracúvajúci viac variant-jobov, alebo `InterventionImageManipulator::watermark()` volajúce `SourceReader` druhýkrát v rámci toho istého generovania cez zdieľaný `progressive_image.filesystem.loader` singleton) sa predchádzajúci file handle nezatvára deterministicky cez `fclose()`, len implicitne cez refcounting/GC — riziko vyčerpania file descriptorov pri intenzívnom opakovanom použití. *(Fix: `load()` teraz zavolá `fclose($this->file)` na predchádzajúci handle, ak je ešte otvorený, pred otvorením nového; pokryté testom `testLoadClosesThePreviousHandleWhenReusedForAnotherFile`.)*

## src/Command

- [OPRAVENÉ] **GenerateCustomCssCommand.php:88-89** — `$content = "/* ... */\n";` hneď nasledované `$content = "@layer vendor {\n";` — druhé priradenie prepíše prvé namiesto zreťazenia (`.=`). Úvodný komentár sa tak nikdy nedostane do vygenerovaného CSS súboru, čo je viditeľná logická chyba. *(Fix: druhé priradenie zmenené na `.=`; pokryté testom `testGeneratedCssKeepsTheLeadingCommentInsteadOfDiscardingIt`.)*
- [OPRAVENÉ] **GenerateCustomCssCommand.php** — konfiguračné kľúče `columns` a `max_container` sú deklarované v type-hinte konštruktora (`$gridConfig`), ale nikde v `generateCssContent()` ani inde v triede sa nepoužívajú. Buď je funkcionalita (grid stĺpce, max-width kontajnera) nedokončená a ticho sa ignoruje, alebo je konfigurácia mŕtva — v oboch prípadoch zavádzajúce pre používateľa bundle, ktorý tieto hodnoty nastaví a nič sa nestane. *(Fix: `max_container` sa teraz premieta do vygenerovaného CSS ako `max-width: {N}px;` v každom breakpoint bloku, kde nie je `null` — presne zodpovedá názvu existujúceho (predtým nefunkčného) testu `testExecuteWithMaxWidthForSmallestBreakpoint`, ktorý bol aktualizovaný. `columns` zostáva nepoužité touto triedou zámerne — je to skutočne živý config kľúč, len ho spotrebúva `ResponsiveAttributeGenerator` pri per-request pixel výpočte, nie tento statický CSS generátor; phpdoc pri konštruktore to teraz explicitne vysvetľuje namiesto toho, aby tváril, že trieda `columns` používa. Pokryté testami `testGeneratedCssIncludesMaxWidthFromMaxContainer` a `testGeneratedCssOmitsMaxWidthWhenMaxContainerIsNull`.)*
- [OPRAVENÉ] **GenerateCustomCssCommand.php:154-202** — `generateVariableFallback()` a `generateVariableFallbackForBreakpoint()` sú takmer identická duplicitná logika (zoraďovanie, skladanie reťazca `var(--img-...)` s vnorenými zátvorkami) s drobnými nekonzistentnými rozdielmi — prvá metóda oddeľuje fallbacky `",\n\t\t\t"`, druhá `', '` bez zalomenia riadku. Duplicita zvyšuje riziko, že sa pri budúcej úprave opraví len jedna vetva a vznikne rozdielne správanie (už teraz je formátovanie nekonzistentné). *(Fix: obe metódy teraz delegujú na novú zdieľanú `buildVariableFallbackChain()`, parametrizovanú separátorom — žiadna zmena výstupu, len jeden zdroj pravdy; existujúce testy overujú presný CSS výstup nezmenený.)*
- [OPRAVENÉ] **GenerateCustomCssCommand.php:59-68** — `Filesystem` sa vytvára priamo cez `new Filesystem()` vnútri `execute()` namiesto injektovania cez konštruktor; robí triedu ťažšie testovateľnou a je to nekonzistentné s DI prístupom zvyšku bundle. *(Fix: `Filesystem` je teraz injektovaný cez konštruktor (`= new Filesystem()` default, PHP 8.1 "new in initializers"), `execute()` používa `$this->filesystem`; pokryté testom `testFilesystemIsInjectedInsteadOfInstantiatedInline` s mockom.)*
- [OPRAVENÉ] **GenerateCustomCssCommand.php:53-57** — argument `path` sa použije priamo na zostavenie cesty súboru (`$path.'/progressive-image-custom.css'`) bez akejkoľvek validácie/sanitizácie (napr. `..` segmenty); pri CLI nástroji je riziko nízke, ale chýba akákoľvek kontrola. *(Fix: `execute()` teraz odmietne argument obsahujúci `..` a vráti `Command::FAILURE` s chybovou hláškou; pokryté testom `testRejectsPathArgumentContainingParentDirectoryTraversal`.)*

## src/Twig/Components/Image.php (analyzované predčasne kvôli PathDecoratorInterface)

- [OPRAVENÉ] **Image.php:94** — `$this->metadata->width ?? 0` používa priamy operátor `->` namiesto nullsafe `?->` na možno-null objekte `$this->metadata` (môže byť `null`, pozri riadok 85-87 po zachytení `PathResolutionException`). Vyvolá to PHP warning „Attempt to read property on null" pri každom obrázku bez zistiteľných metadát, aj keď `??` nakoniec doplní `0`. O pár znakov ďalej na tom istom riadku sa pre `height` správne používa `$this->metadata?->height ?? 0` — nekonzistentnosť v rámci jedného riadku ukazuje, že ide o prehliadnutú chybu, nie zámer. *(Fix: obe hodnoty sa teraz čítajú cez explicitné `null !== $metadata ? $metadata->width : 0` z lokálnej premennej — nielen nullsafe operátor, pretože kombinácia `?->` + `??` na tej istej premennej narazila na známy PHPStan flow-analysis bug, ktorý po prvom takom výraze mylne označí ďalšie nullsafe prístupy k tej istej premennej ako "nikdy null"; explicitný `null !==` test je spoľahlivý pre statickú aj behovú analýzu. Pokryté testom `testPostMountDoesNotEmitWarningWhenMetadataIsNullAndResponsiveAttributesAreGenerated`.)*
- [OPRAVENÉ] **Image.php:79-81 a 98-104** — `$this->pathDecorator` (typ `iterable`) sa prechádza cyklom `foreach` dvakrát v tej istej metóde (`postMount()`). Ak konzumujúca aplikácia poskytne jednorazový `\Generator` namiesto poľa/`RewindableGenerator`, druhé volanie `getSize()` prebehne nula-krát bez chyby a `decoratedWidth`/`decoratedHeight` zostanú len z metadát analyzátora — tichý, ťažko odhaliteľný bug závislý od typu injektovanej implementácie. *(Fix: `postMount()` teraz na začiatku zmaterializuje `$this->pathDecorator` do poľa (`iterator_to_array` ak to už nie je pole) a obe slučky používajú toto pole. Test s reálnym jednorazovým generátorom ukázal, že skutočný dopad je horší než "tichý bug" — pôvodný kód pri druhom prechode vyhadzoval nezachytenú `Exception: Cannot traverse an already closed generator`. Pokryté testom `testPathDecoratorIterableIsOnlyConsumedOnceEvenThoughUsedTwice`.)*
- [INVESTIGOVANÉ, bez zmeny] **Image.php:84 vs. 78-81** — metadáta sa čítajú z pôvodného `$this->src` (`$this->analyzer->getMetadata($this->src)`), zatiaľ čo zobrazovaná/použitá cesta je `$this->decoratedSrc` prechádzajúca cez `PathDecoratorInterface::decorate()`. Ak dekorátor mapuje logickú cestu na inú fyzickú/URL cestu (na to evidentne slúži), analyzátor môže čítať metadáta zo zdroja, ktorý sa nezhoduje s tým, čo sa reálne vykreslí, alebo zlyhá na ceste, ktorú dekorátor mal práve opraviť. *(Overené: toto je zámerné oddelenie zodpovedností, nie bug. `PathDecoratorInterface::decorate()` produkuje verejnú/render URL (napr. `FakeFilterPathDecorator` produkuje `http://localhost/media/cache/resolve/...`), ktorú `PathResolverInterface`/`MetadataReader` nedokáže resolvovať ako súbor na disku — sú to dva nezávislé, zámerne oddelené mechanizmy (jeden pre čítanie bajtov obrázka, druhý pre zobrazovanú URL). Existujúci test `testComponentProperties` explicitne overuje `getMetadata()->with('test.jpg')` (pôvodný `$this->src`, nie dekorovaná URL) — zmena zdroja metadát na `decoratedSrc` by tento kontrakt porušila a s reálnym dekorátorom by zlyhala. Ponechané bez zmeny.)*
- [OPRAVENÉ] **Image.php:50** — vlastnosť `private array $breakpoinsts` má preklep v názve (má byť „breakpoints"). Používa sa konzistentne v rámci triedy, takže funkčne to nespôsobí chybu, ale sťažuje čitateľnosť/vyhľadateľnosť kódu a pôsobí neprofesionálne v public-facing bundle. *(Fix: premenované na `$breakpoints` na všetkých výskytoch v triede.)*
- [OPRAVENÉ] **Image.php:85** — `catch (PathResolutionException) { $this->metadata = null; }` je úplne tichý catch blok — žiadne logovanie, žiadna metrika, nič. `MetadataReaderInterface::getMetadata()` môže túto výnimku vyhodiť pri systémovej chybe konfigurácie (napr. zle nastavený resolver root pre celú aplikáciu, nie len jeden obrázok), a keďže sa tu ticho prehltne pre KAŽDÝ jeden `<img>` na stránke bez akejkoľvek stopy, plošná chyba konfigurácie sa navonok prejaví len ako "obrázky nemajú blurhash/rozmery" bez čo i len jedného riadku v logu, ktorý by naznačil prečo. *(Fix: pridaný nullable `?LoggerInterface $logger` konštruktorový parameter (rovnaký vzor ako `ImageVariantController`, DI cez `IGNORE_ON_INVALID_REFERENCE`), catch blok teraz volá `$this->logger?->warning(...)` s pôvodnou cestou a dôvodom. Pokryté testom `testComponentLogsWhenMetadataResolutionFails`.)*

## src/DependencyInjection

- [OPRAVENÉ] **Configuration.php:24-35** — `beforeNormalization()->ifArray()->then(...)` obsahuje `if` blok, ktorého telo je len komentár („Possibility to automatically create a default resolver if necessary, or leave for validation later.") — nič sa v ňom nevykoná, `$v` sa vždy vráti nezmenené. Mŕtvy/nedokončený kód, ktorý pôsobí, akoby normalizoval `resolver`, no v skutočnosti nerobí nič. *(Fix: celý no-op `beforeNormalization` blok odstránený. Skutočná voľba defaultného resolvera je riešená v `ProgressiveImageExtension::configureResolvers()` — pozri opravu nižšie v sekcii `src/DependencyInjection/ProgressiveImageExtension.php`.)*
- [OPRAVENÉ] **Configuration.php:88-97** — blok `retina` je odsadený tabulátormi, zatiaľ čo zvyšok súboru používa medzery (a inde nekonzistentné hĺbky odsadenia, napr. `->scalarNode('resolver')` na riadku 75 je posunuté o úroveň menej než susedné uzly). Nekonzistentné odsadenie naznačuje kopírovanie z iného zdroja/formátovača a sťažuje orientáciu vo vnorenej štruktúre config stromu. *(Fix: `retina` blok prevedený z tabulátorov na 4-medzerové odsadenie zhodné so zvyškom súboru; `->scalarNode('resolver')` zarovnaný na úroveň susedných uzlov; opravené aj ďalšie dva výskyty rovnakého problému nájdené pri prechode celého súboru (komentáre `// for filesystem/chain resolver` a blok `image_configs`). Overené cez `php-cs-fixer --dry-run` (0 súborov na opravu) a existujúcou `ConfigurationTest` sadou.)*
- [INVESTIGOVANÉ, bez zmeny] **Configuration.php:105-143** — `beforeNormalization` pre `responsive_strategy.grid` zlučuje predvolené hodnoty frameworku (`bootstrap`/`tailwind`) s používateľovou konfiguráciou cez `array_replace_recursive($defaults, $v)`. Keďže ide o asociatívne zlúčenie, ak používateľ nastaví `framework: tailwind` a zároveň definuje VLASTNÉ `layouts` s inými názvami breakpointov (napr. namiesto premenovania `2xl`/`xl`/... si vytvorí vlastné kľúče), výsledkom nie je nahradenie predvolených layoutov, ale ich zjednotenie — používateľ dostane tailwind defaulty AJ svoje vlastné breakpointy naraz, čo nie je nikde zdokumentované a je prekvapivé správanie. *(Overené: toto je zámerné, otestované správanie, nie bug. Existujúci test `ConfigurationTest::testOverrideFrameworkDefaults` explicitne definuje IBA `md` a `custom` breakpointy a s komentárom `// Preserved xxl` overuje, že `xxl` z bootstrap defaultov ostáva zachované — presne merge-a-nie-replace sémantika, ktorú by "oprava" tejto položky zničila. Zmena na plné nahradenie pri explicitnom `layouts` kľúči by porušila tento existujúci, zámerný kontrakt. Ponechané bez zmeny.)*

## src/DependencyInjection/CheckCacheInterfacePass.php

- [OPRAVENÉ] **CheckCacheInterfacePass.php:53-55** — vetva `if (!$class && $definition->getFactory())` obsahuje iba komentár bez akéhokoľvek kódu. Keď je cache služba definovaná cez factory a `$class` sa nedá zistiť, validácia sa ticho preskočí — nasledujúci `if ($class && ...)` na riadku 57 je automaticky `false`, takže sa nevyhodí žiadna výnimka a pass propustí aj cache pool, ktorý v skutočnosti neimplementuje `TagAwareCacheInterface`. Účel triedy (zachytiť zlú cache konfiguráciu pri `cache:clear`, nie za behu) tak zlyhá práve pre factory-definované služby. *(Pri vyšetrovaní sa ukázalo, že factory prípad NIE JE ticho ignorovaný — `!$class` už spadá do druhej (tag-based) kontroly nižšie. Pri písaní testu na to sa ale odhalil skutočný, závažnejší bug: druhá kontrola (predtým riadky 61-69) bola v skutočnosti nedosiahnuteľný mŕtvy kód pre `ArrayAdapter`/`FilesystemAdapter` triedy, pretože PRVÁ kontrola (`is_subclass_of`) ich vždy zamietne skôr, než sa vôbec dostane k tag-based fallbacku — obe triedy zlyhávajú `is_subclass_of(..., TagAwareCacheInterface)`. Fix: poradie kontrol preusporiadané — "provizórna" trieda (prázdna/ArrayAdapter/FilesystemAdapter) sa teraz kontroluje PRED prísnou `is_subclass_of` kontrolou, takže `cache.taggable` tag fallback je konečne skutočne dosiahnuteľný pre všetky tri scenáre (factory, ArrayAdapter, FilesystemAdapter). Odstránený aj mŕtvy komentárový `if` blok. Pokryté 4 testami v novom `CheckCacheInterfacePassTest`.)*
- [OPRAVENÉ] **CheckCacheInterfacePass.php:63** — natvrdo zapísané reťazce tried `'Symfony\Component\Cache\Adapter\ArrayAdapter'` a `'...\FilesystemAdapter'` namiesto `::class` konštánt. Statická analýza (PHPStan) ani IDE nevie skontrolovať, že tieto triedy skutočne existujú alebo že názov nebol preklepnutý; pri premenovaní tried v budúcej verzii Symfony by táto vetva ticho prestala fungovať bez akéhokoľvek varovania pri kompilácii. *(Fix: nahradené `ArrayAdapter::class`/`FilesystemAdapter::class` s importom na začiatku súboru.)*

## src/DependencyInjection/ProgressiveImageExtension.php

- [OPRAVENÉ] **ProgressiveImageExtension.php:197 vs. Configuration.php:98-105** — podmienka `if ($generatorId || isset($responsiveConfig['grid']))` je fakticky vždy `true`. Uzol `responsive_strategy` aj jeho podstrom `grid` majú v `Configuration.php` nastavené `addDefaultsIfNotSet()`, takže po `processConfiguration()` je kľúč `grid` VŽDY prítomný (aj keď ho používateľ v konfigurácii vôbec nezadal). Zámer kódu — registrovať `ResponsiveAttributeGenerator` a URL generátor iba „keď je nakonfigurovaný" responsive_strategy — je tak nefunkčný: služby sa registrujú vždy, bez ohľadu na to, či si to používateľ bundle vôbec praje. *(Fix: obidva výskyty tejto vždy-pravdivej podmienky (aj analogický duplikát pri `Image::$responsiveAttributeGenerator`) odstránené, `ResponsiveAttributeGenerator` sa registruje bezpodmienečne — bez zmeny reálneho správania, len bez zavádzajúcej podmienky. Pokryté testom `testResponsiveAttributeGeneratorIsRegisteredEvenWithoutExplicitResponsiveStrategyConfig`.)*
- [OPRAVENÉ] **ProgressiveImageExtension.php:235-237 vs. Configuration.php:101-104** — dokumentačný `->info()` pre `responsive_strategy.generator` explicitne hovorí: „Overrides the default/Variant-pipeline generator when set." V skutočnosti, keď je nastavené aj `variant_store.storage` AJ vlastný `responsive_strategy.generator`, kód na konci `load()` bezpodmienečne prepíše alias `ResponsiveImageUrlGeneratorInterface` na `VariantResponsiveImageUrlGenerator::class` bez kontroly, či `$generatorId` bol nastavený — presný opak toho, čo tvrdí popis konfiguračnej voľby. Vlastný generátor je ticho ignorovaný namiesto toho, aby prevážil nad Variant pipeline. *(Fix: podmienka rozšírená na `!$generatorId && ...`. Pri písaní testu sa odhalil súvisiaci gap: keď bol `$generatorId` nastavený BEZ `variant_store.storage`, verejný alias `ResponsiveImageUrlGeneratorInterface` sa vôbec neregistroval (nikto ho priamo nepotreboval, keďže `ResponsiveAttributeGenerator` používa `$generatorId` priamo), takže hocikto iný, kto by si alias vypýtal z kontajnera priamo, by dostal `ServiceNotFoundException`. Teraz sa alias vždy nastaví na efektívne aktívny generátor (explicitný > Variant pipeline > default). Pokryté testami `testExplicitResponsiveStrategyGeneratorWinsOverTheVariantPipeline` a `testVariantPipelineWinsOverTheDefaultGeneratorWhenNoCustomGeneratorIsConfigured`.)*
- [OPRAVENÉ] **ProgressiveImageExtension.php:266-284 (`configureResolvers`)** — keď používateľ nedefinuje `resolver` explicitne (default `'default'`) a zároveň má vlastné pomenované `resolvers`, ale žiadny sa nevolá doslovne `default`, vyberie sa `array_key_first($resolvers)` — teda „prvý resolver v poradí, ako je zapísaný v configu" sa potichu stane predvoleným. Toto poradie-závislé magické správanie nie je nikde zdokumentované; obyčajné preusporiadanie/pridanie resolvera na začiatok YAML konfigurácie tak môže nečakane zmeniť, ktorý resolver sa použije ako default, bez akejkoľvek chyby alebo varovania. *(Fix: implicitný fallback na "prvý resolver" ostáva iba pre jednoznačný prípad presne jedného nakonfigurovaného resolvera; pri 2+ resolveroch bez `resolver:` a bez kandidáta nazvaného `default` teraz `cache:clear`/boot zlyhá s jasnou `LogicException`, ktorá vymenuje dostupné resolvery a žiada explicitné nastavenie. Pokryté testom `testAmbiguousDefaultResolverWithMultipleNamedResolversFailsFast`.)*
- [OPRAVENÉ] **ProgressiveImageExtension.php:189** — `new \Symfony\Component\DependencyInjection\Argument\TaggedIteratorArgument(...)` použité s plne kvalifikovaným názvom triedy, hoci tá istá trieda je na riadku 18 importovaná (`use ... TaggedIteratorArgument;`) a inde v súbore (riadok 372) sa používa skrátený import. Nekonzistentnosť v rámci jedného súboru naznačuje kopírovanie kódu bez zjednotenia štýlu. *(Fix: nahradené skráteným `TaggedIteratorArgument` zhodne s riadkom 388.)*

## src/DTO

- [OPRAVENÉ] **BreakpointAssignment.php:99-102 (`parseSegments`)** — `explode(' ', $segments)` delí reťazec presne podľa jednej medzery bez `trim()`/filtrovania prázdnych položiek. Bežný preklep v šablóne (dvojitá medzera, medzera na konci `sizes="lg:4 md:6 "`) vyprodukuje prázdny segment `''`, ktorý v `fromSegment()` neprejde žiadnou z podmienok (nie je `[...]`, `is_numeric('')` je `false`) a skončí vo `else` vetve na riadku 78 vyhodením `\InvalidArgumentException`. Bežný preklep v atribúte tak zhodí celé renderovanie namiesto tichého ignorovania prázdneho segmentu. *(Fix: `explode(' ', ...)` nahradené `preg_split('/\s+/', trim($segments), -1, PREG_SPLIT_NO_EMPTY)` — toleruje viacnásobné/okrajové medzery. Pokryté testom `testParseSegmentsIgnoresExtraWhitespaceInsteadOfThrowing`.)*
- [OPRAVENÉ] **BreakpointAssignment.php:67-69** — pri formáte `[WIDTHxHEIGHT]` sa `$widthStr`/`$heightStr` pretypujú priamo cez `(int)` bez overenia, že sú číselné. Neplatný vstup typu `[abcxdef]` sa ticho vyhodnotí ako `width=0, height=0` namiesto vyhodenia výnimky — nekonzistentné so susednou vetvou na riadku 78, ktorá pri inak nerozpoznanom formáte segmentu výnimku vyhodí. Rovnaký typ chyby (nezmyselný vstup) je raz tichý, raz fatálny, podľa toho, ktorou vetvou prejde. *(Fix: pridaná `is_numeric()` validácia oboch častí pred `(int)` pretypovaním, s rovnakou `InvalidArgumentException` správou ako ostatné nerozpoznané formáty. Pokryté testami `testFromSegmentThrowsForNonNumericExplicitDimensions` a `testFromSegmentThrowsForPartiallyNonNumericExplicitDimensions`.)*

## src/Event

- [OPRAVENÉ] **KernelResponseEventListener.php:24-64 vs. src/Service/PreloadCollector.php:44-57** — `PreloadCollector::add()` už registruje preload odkazy cez oficiálny mechanizmus `symfony/web-link` (`Link`/`GenericLinkProvider` na atribúte requestu `_links`), ktorý Symfony sám automaticky premení na HTTP `Link` hlavičku (balík `symfony/web-link` je tvrdá závislosť v `composer.json`). `KernelResponseEventListener` navyše RUČNE znovu zostavuje `Link` hlavičku z tých istých dát (`$this->preloadCollector->getUrls()`) a pridáva ju cez `$response->headers->set('Link', ..., false)` (parameter `false` = pridať, nie nahradiť). Výsledok: pre každý preloadovaný obrázok sa do odpovede pošle `Link` hlavička DVAKRÁT, raz od Symfony WebLink poslucháča a raz od tohto bundle — duplicitné, mierne odlišne formátované preload inštrukcie v tej istej HTTP odpovedi. *(Fix: overené (`vendor/symfony/framework-bundle/Resources/config/web_link.php` registruje `AddLinkHeaderListener` automaticky, keď je `symfony/web-link` nainštalovaný) a celý ručný blok stavajúci `Link` hlavičku odstránený — listener teraz robí iba HTML `<link>` injection, čo je jeho jediná unikátna zodpovednosť. Existujúce testy, ktoré volali listener priamo a čakali `Link` hlavičku, prerobené tak, aby popri ňom spúšťali aj skutočný `AddLinkHeaderListener` — presne ako to robí reálny `kernel.response` event — a overili, že hlavička stále vznikne správne end-to-end.)*
- [OPRAVENÉ] **KernelResponseEventListener.php:47-63** — telo odpovede (`$response->getContent()`) sa upravuje cez `str_replace('</head>', ..., $content)` bez akejkoľvek kontroly `Content-Type` alebo `$event->isMainRequest()`. Pre `StreamedResponse`/`BinaryFileResponse` vráti `getContent()` `false`, ktoré sa ticho pretypuje na prázdny reťazec; pri sub-requestoch (ESI/forward) alebo ne-HTML odpovediach (JSON/XML/CSV) sa listener spúšťa rovnako a v krajnom prípade (výskyt reťazca `</head>` v texte non-HTML odpovede) by vložil HTML fragment do nesprávneho typu obsahu. Chýba štandardná poistka „iba pre hlavný request a iba pre `text/html`", ktorú majú podobné Symfony listenery. *(Fix: pridaná `$event->isMainRequest()` poistka, kontrola `Content-Type` (preskočí, len ak je EXPLICITNE nastavený na niečo iné než `text/html` — v momente `kernel.response` bežná HTML odpoveď z Twig kontroléra typicky ešte nemá `Content-Type` nastavený, `Response::prepare()` beží až neskôr, takže "nenastavené" sa berie ako HTML) a `false === $content` guard pre `StreamedResponse`/`BinaryFileResponse`. Pokryté testami `testSkipsSubRequests`, `testSkipsResponsesWithAnExplicitNonHtmlContentType`, `testSkipsResponsesWithoutStringContent`.)*
- [OPRAVENÉ] **KernelResponseEventListener.php:35-58** — hodnoty `$url`, `$attr['as']`, `$attr['imagesrcset']`, `$attr['imagesizes']` sa vkladajú do `Link` hlavičky aj do `<link ...>` HTML tagu cez `sprintf` bez akéhokoľvek escapovania (`htmlspecialchars` a pod.). Ak niektorá z hodnôt (napr. cesta k obrázku odvodená z názvu nahraného súboru) obsahuje úvodzovku, dôjde k rozbitiu HTML atribútu / vloženiu ľubovoľného markupu do `<head>`. *(Fix: keďže ručné budovanie `Link` hlavičky je preč, zostáva iba HTML `<link>` tag — všetky 4 hodnoty teraz prechádzajú cez `htmlspecialchars(..., ENT_QUOTES)`. Pokryté testom `testEscapesHtmlSpecialCharactersInPreloadAttributes`.)*

## src/Event/TransparentImageCacheSubscriber.php + src/Twig/TransparentCacheExtension.php (súvisiaci mechanizmus, analyzované spolu)

- [OPRAVENÉ] **TransparentImageCacheSubscriber.php:43 vs. :67** — `onPreCreate()` počíta cache kľúč z `$event->getInputProps()` (surové atribúty zadané v šablóne, PRED spustením `PostMount`/naplnením defaultov), zatiaľ čo `onPreRender()` počíta kľúč z `$event->getVariables()` (plná sada template premenných PO mounte, vrátane defaultných hodnôt vlastností ako `retina`, `priority`, `framework`, `preload`, ktoré nemuseli byť súčasťou pôvodných vstupných props). Keďže `generateKey()` je `md5(serialize($vars))`, tieto dve sady takmer nikdy nebudú identické pre bežné použitie komponentu (napr. `<twig:pgi:Image src="a.jpg"/>` bez explicitne zadaných voliteľných atribútov) — kľúč vypočítaný pri hľadaní v cache (`onPreCreate`) sa reálne nezhoduje s kľúčom, pod ktorým sa neskôr ukladá vyrenderovaný obsah (`onPreRender` → `pgi_cache_save` → `TransparentCacheExtension::saveToCache()`). Transparentná cache tak s vysokou pravdepodobnosťou nikdy nezasiahne (cache hit), hoci sa navonok tvári, že komponent kešuje. *(Fix: overené v `vendor/symfony/ux-twig-component/src/ComponentFactory.php::mountFromObject()` a `ComponentRenderer::createAndRender()`, že `MountedComponent::getInputProps()` vracia presne to isté pole `$props`, aké dostal aj `PreCreateForRenderEvent::getInputProps()` — obe pochádzajú z toho istého volania `createAndRender($name, $props)`, ešte pred akoukoľvek mutáciou defaultami. `onPreRender()` teraz počíta kľúč z `$event->getMountedComponent()->getInputProps()` namiesto z `$event->getVariables()`, takže lookup aj save kľúč vychádzajú zo zhodných dát. Pokryté testom `testSecondRequestForTheSameComponentGetsACacheHitWithTheRealHtml`.)*
- [OPRAVENÉ] **TransparentImageCacheSubscriber.php:45-52** — aj keby sa kľúče zhodovali, callback odovzdaný do `$this->cache->get($key, function (...) { ...; return null; })` VŽDY vracia `null`. Symfony cache (`TagAwareCacheInterface::get`) uloží návratovú hodnotu callbacku ako platnú keš položku vrátane `null` a považuje ju za „hit" pri ďalšom volaní so zhodným kľúčom — čo znamená, že do cache sa natrvalo uloží prázdna (`null`) hodnota už pri prvom volaní `onPreCreate()`. Následné volanie `TransparentCacheExtension::saveToCache()` (riadok 49 v `TransparentCacheExtension.php`), ktoré má uložiť skutočný vyrenderovaný HTML pod tým istým kľúčom cez `$this->cache->get($key, ...)`, nájde už existujúci (null) záznam a svoj callback vôbec nezavolá — reálny obsah sa tak nikdy neuloží do cache. Kombinácia oboch nálezov robí z „transparentnej cache" komponentu de facto no-op, ktorý pri každom requeste znovu prepočíta a vyrenderuje celý komponent. *(Fix: `onPreCreate()` už nepoužíva `get()` s vždy-null callbackom — namiesto toho robí čistý lookup cez `getItem($key)`/`isHit()`, ktorý pri cache-miss do cache nič nezapíše (write robí výlučne `TransparentCacheExtension::saveToCache()` po skutočnom vyrenderovaní). Vyžaduje to, aby injektovaný cache pool implementoval aj `Psr\Cache\CacheItemPoolInterface` popri `TagAwareCacheInterface` — konštruktor teraz používa intersection type `(CacheItemPoolInterface&TagAwareCacheInterface)|null $cache`; všetky reálne symfony/cache adaptéry (`TagAwareAdapter`, `AbstractTagAwareAdapter`) obe rozhrania implementujú. Pokryté testami `testSecondRequestForTheSameComponentGetsACacheHitWithTheRealHtml` a `testPreCreateDoesNotPoisonTheCacheWithANullEntryOnAMiss`.)*

## src/Service/PreloadCollector.php

- [OPRAVENÉ] **PreloadCollector.php:30-58** — `add()` ukladá interné pole `$this->urls[$url] = [...]` (kľúčované podľa URL, teda opakované volanie s rovnakým `$url` prepíše predchádzajúci záznam), no zároveň pri každom volaní bez podmienky pridáva nový `Link` objekt do WebLink providera (`$linkProvider->withLink($link)`, riadok 57) bez kontroly, či pre rovnaké `$url` už `Link` existuje. Ak sa rovnaký obrázok preloadne dvakrát v jednom requeste (napr. dvakrát vyrenderovaná rovnaká komponenta), interné pole `$urls` bude mať len jeden (posledný) záznam, ale WebLink provider bude obsahovať dva samostatné `Link` objekty pre tú istú URL — nekonzistentné správanie medzi dvoma mechanizmami preloadovania v tej istej triede. *(Fix: pred `withLink()` sa teraz odstránia všetky existujúce `Link` objekty s rovnakým `getHref()` cez `withoutLink()`, takže druhé volanie pre tú istú URL nahradí, nie len pridá. Pokryté testom `testAddingTheSameUrlTwiceKeepsOnlyOneLinkInTheWebLinkProvider`.)*

## src/Modifier

- [OPRAVENÉ] **ModifierProvider.php:30-41 (`applyModifiers`)** — vonkajší cyklus prechádza `$modifierStrings` a pre KAŽDÝ z nich znovu prechádza `$this->modifiers` (typ `iterable`, v praxi injektovaný cez `TaggedIteratorArgument`, pozri `ProgressiveImageExtension.php:189`). Rovnaký vzor ako pri `Image.php` (`$pathDecorator`) — ak by `$this->modifiers` bol jednorazový `\Generator` namiesto pretočiteľného iterátora, po prvom prejdení `$modifierStrings` by sa pre všetky ďalšie stringy neaplikoval žiadny modifikátor. Typový hint `iterable` túto vlastnosť nijako nevynucuje ani nedokumentuje — druhý výskyt rovnakého rizikového vzoru v kóde naznačuje systematický, nie náhodný problém. *(Fix: rovnaké riešenie ako v `Image.php` — konštruktor teraz materializuje `$modifiers` do `array` (`iterator_to_array($modifiers, false)`, ak to už nie je `array`) presne raz, takže sa dá bezpečne pretáčať v každej iterácii vonkajšieho cyklu. Pokryté testom `testModifiersIterableIsOnlyConsumedOnceEvenWhenAppliedToMultipleModifierStrings`, ktorý injektuje jednorazový `\Generator` a overí, že sa aplikujú modifikátory pre VŠETKY `$modifierStrings`, nielen pre prvý.)*

## src/Resolver

- [OPRAVENÉ] **FileSystemResolver.php:109-124 (`sanitizeAbsolutePath`)** — bezpečnostná kontrola „je cesta v rámci povoleného root adresára" používa `0 === mb_strpos($path, $root)`, teda čisté porovnanie prefixu reťazca. To je klasická zraniteľnosť: root `/var/www/public` a vyriešená cesta `/var/www/public-secret/passwd` (súrodenský adresár, NIE podadresár) prejde kontrolou, pretože reťazec `/var/www/public-secret/...` začína reťazcom `/var/www/public`. Správne by sa malo porovnávať s explicitným oddeľovačom na konci roota (napr. `str_starts_with($path, rtrim($root, '/').'/')`). Ide o (znovu)zavlečenú zraniteľnosť z pôvodného LiipImagineBundle kódu (viď hlavička súboru), ktorá umožňuje čítanie súborov mimo nakonfigurovaných root adresárov, ak na disku existuje súrodenský adresár so zhodným prefixom názvu. *(Fix: presne navrhnutá zmena — `str_starts_with($path, rtrim($root, '/').'/')`. Pokryté novým testom `testResolveRejectsPathsInASiblingDirectorySharingTheRootsNamePrefix`, ktorý vytvorí skutočný súrodenský adresár `{root}-secret/` na disku a overí, že `resolve('../{root}-secret/secret.txt')` teraz vyhodí `PathResolutionException` namiesto úspešného vyriešenia — pred opravou tento test reálne zlyhával (žiadna výnimka).)*
- [INVESTIGOVANÉ, bez zmeny] **FileSystemResolver.php:93-104 (`sanitizeRootPath`)** — pri `$allowUnresolvable = true` a zlyhanom `realpath()` (napr. adresár ešte neexistuje) sa ako root použije surová, nekanonizovaná `$path`. Táto nekanonizovaná hodnota sa neskôr porovnáva jednoduchým prefixovým testom (pozri predchádzajúci nález) proti kanonizovanej (`realpath()`) absolútnej ceste — nekonzistentné porovnávanie kanonickej a nekanonickej cesty ešte viac oslabuje už aj tak nespoľahlivú kontrolu root hraníc. *(Overené: `generateAbsolutePath()` vracia hodnotu LEN ak `realpath($root.'/'.$path)` úspešne uspeje — inak vráti `null` a `sanitizeAbsolutePath()` sa vôbec nezavolá. To znamená, že `sanitizeAbsolutePath()` vždy dostáva plne kanonickú (symlinky aj `..` segmenty rozpustené) absolútnu cestu k SKUTOČNE existujúcemu súboru. Ak je uložený root nekanonický (napr. obsahuje `..` alebo je relatívny), výsledný string-prefix porovnanie voči tejto kanonickej ceste sa môže nanajvýš NEZHODOVAŤ (legitímny súbor by bol nesprávne odmietnutý — funkčný, nie bezpečnostný problém), nikdy sa však nemôže zhodovať NAVYŠE oproti tomu, čo by dovolil kanonický root — teda nekanonický root neotvára žiadnu ďalšiu únikovú cestu nad rámec toho, čo už rieši predchádzajúci fix `str_starts_with(...)`. Skutočná kanonizácia cesty, ktorá na disku ešte neexistuje, navyše nie je technicky možná (realpath() ju z princípu nevie vyriešiť). Ponechané bez zmeny.)*
- [OPRAVENÉ] **ProgressiveImageExtension.php:252 (súvisí s `Configuration.php:50`)** — `$resolverConfig['allowUnresolvable'] ?? true` pôsobí, akoby default bol `true`, no `Configuration.php` definuje `->booleanNode('allowUnresolvable')->defaultFalse()`. Keďže Symfony config strom po `processConfiguration()` má tento kľúč VŽDY prítomný (s hodnotou `false`, ak used nezadal inak), časť `?? true` sa nikdy nemôže uplatniť — mŕtvy, zavádzajúci kód, ktorý pri čítaní vyvoláva nesprávny dojem o skutočnom predvolenom správaní. *(Fix: `?? true` odstránené, priamo `$resolverConfig['allowUnresolvable']`. Pokryté novým testom `testNamedFilesystemResolverDefaultsAllowUnresolvableToFalse`, ktorý dokazuje, že vynechanie `allowUnresolvable` v konfigurácii vedie k `false` správaniu (vyhodí `\InvalidArgumentException` pre neexistujúci root) — test prechádzal aj PRED touto zmenou, čím potvrdzuje, že `?? true` bol skutočne nedosiahnuteľný mŕtvy kód, nie funkčná zmena správania.)*
- [OPRAVENÉ] **AssetMapperResolver.php:27-31 (`resolve`)** — pri každom volaní sa lineárne prechádza `$this->assetMapper->allAssets()` (O(n) nad všetkými mapovanými assetmi) bez akéhokoľvek indexu/cache podľa `publicPath`. Keďže resolver sa typicky volá raz na obrázok pri renderovaní stránky, pri väčšom počte assetov (bežné pri asset_mapper manifeste s tisíckami súborov) ide o zbytočne opakovanú lineárnu prácu na každý jeden `<img>`. *(Fix: `resolve()` teraz lazy postaví `publicPath => sourcePath` index raz (pri prvom volaní) a uloží ho do inštančnej property; každé ďalšie volanie robí O(1) `isset()` lookup namiesto opätovného volania `allAssets()`. Pokryté testom `testResolveBuildsThePublicPathIndexOnlyOnceAcrossMultipleCalls`, ktorý cez mock overuje `allAssets()` volané presne raz naprieč dvomi `resolve()` volaniami.)*
- [OPRAVENÉ] **ChainResolver.php:22 a 28** — `$this->resolvers` (typ `iterable`) sa prechádza v `resolve()`, ktoré sa typicky volá opakovane (raz na obrázok) počas jedného requestu. Ide o tretí výskyt rovnakého vzoru ako pri `Image.php` (`$pathDecorator`) a `ModifierProvider.php` (`$modifiers`) — ak by injektovaná implementácia bola jednorazový `\Generator`, druhé a každé ďalšie volanie `resolve()` v tom istom requeste by prešlo cez prázdny iterátor a rovno vyhodilo `PathResolutionException`, aj keby prvé volanie prešlo v poriadku. *(Fix: rovnaké riešenie ako v `Image.php`/`ModifierProvider.php` — konštruktor materializuje `$resolvers` do `array` presne raz. Pokryté testom `testResolversIterableIsOnlyConsumedOnceEvenAcrossMultipleResolveCalls`, ktorý injektuje jednorazový `\Generator` s prvým resolverom, ktorý vždy zlyhá (aby sa reálne zavolal `next()`), a druhým, ktorý uspeje — pred opravou druhé volanie `resolve()` vyhodilo `Cannot rewind a generator that was already run`.)*

## src/Service/MetadataReader.php

- [OPRAVENÉ] **MetadataReader.php:44** — cache kľúč je iba `md5($src)`, bez akéhokoľvek prefixu/menného priestoru bundle. `image_cache_service` je podľa `Configuration.php:77` predvolene zdieľaný `cache.app` pool — teda ten istý pool, ktorý bežne používa aj zvyšok aplikácie. Iné časti TOHTO ISTÉHO bundle si dávajú pozor a kľúče prefixujú (`TransparentImageCacheSubscriber.php` používa `'pgi_comp_'.md5(...)`, `'pgi_tag_'.md5(...)`) — tu prefix chýba, čím vzniká reálne riziko kolízie s ľubovoľným iným kódom aplikácie, ktorý by náhodou tiež použil `md5()` reťazca ako kľúč v tom istom zdieľanom cache poole. *(Fix: kľúč je teraz `'pgi_meta_'.md5($src)`, rovnaká konvencia ako v `TransparentImageCacheSubscriber.php`. Pokryté aktualizovaným testom `testGetMetadataReturnsCachedValue`, ktorý overuje presný prefixovaný kľúč.)*
- [OPRAVENÉ] **MetadataReader.php:44-65** — keď `$this->pathResolver->resolve($src)` zlyhá a je nakonfigurovaný `$fallbackPath`, vypočítané metadáta NÁHRADNÉHO obrázka (vrátane nákladného prepočtu blurhash cez GD/Imagick analyzátor) sa uložia do cache pod kľúčom odvodeným z PÔVODNÉHO (chybného) `$src`. Pre každú unikátnu neexistujúcu/rozbitú cestu k obrázku sa tak znovu vypočíta a samostatne zacachuje identický fallback obrázok — pri stránke/importe s množstvom rôznych rozbitých `src` hodnôt (napr. po migrácii dát) rastie cache neobmedzene a zbytočne sa opakovane spúšťa CPU-náročná analýza obrázka pre stále ten istý fallback. *(Fix: pridaná nová privátna metóda `getFallbackMetadata()`, ktorá cachuje výsledok pod vlastným kľúčom `'pgi_meta_'.md5($this->fallbackPath)` — spoločným pre všetky rozbité `$src`. Pokryté testom `testFallbackMetadataIsComputedOnceAndSharedAcrossDifferentBrokenSources`, ktorý cez reálny `ArrayAdapter` overuje, že `analyze()` sa pre dva RÔZNE rozbité `$src` zavolá iba RAZ.)*
- [OPRAVENÉ] **MetadataReader.php:44-65** — opačný prípad: keď `resolve()` zlyhá a `$fallbackPath` NIE JE nastavený, výnimka sa vyhodí von z cache callbacku. Symfony `CacheInterface::get()` v takom prípade nič neuloží do cache (žiadne "negative caching"), takže pri KAŽDOM ďalšom requeste, ktorý odkazuje na tú istú trvalo chýbajúcu cestu, sa `pathResolver->resolve()` aj `dispatchEvent()` (a teda aj `ImageNotFoundEvent`) spustia odznova, navždy, bez akéhokoľvek cachovania negatívneho výsledku — pri stránke s viacerými trvalo rozbitými obrázkami ide o zbytočnú opakovanú prácu na každý jeden request. *(Fix: cache callback teraz namiesto vyhodenia výnimky vracia `null` ako negatívny marker (validná "úspešná" hodnota pre `cache->get()`, teda sa naozaj uloží) — `getMetadata()` po prečítaní z cache skontroluje `null === $result` a vyhodí čerstvú `PathResolutionException` mimo cachovaného callbacku. Keďže `ImageMetadata` nikdy nie je legitímne `null`, toto je bezpečný marker. Pokryté testom `testUnresolvableSourceWithoutFallbackIsNegativelyCachedAndDoesNotRetryResolution`, ktorý cez reálny `ArrayAdapter` volá `getMetadata()` dvakrát pre ten istý trvalo rozbitý `$src` a overuje, že `pathResolver->resolve()` aj `dispatcher->dispatch()` sa zavolajú iba RAZ.)*

## src/Service/ResponsiveAttributeGenerator.php

- [OPRAVENÉ] **ResponsiveAttributeGenerator.php:92-94** — `$this->preloadCollector->add(...)` sa volá vnútri `foreach ($assignments as $assignment)`, teda raz PRE KAŽDÝ breakpoint, nie raz za celý `<img>`. Pri obrázku s viacerými breakpointami (`sizes="lg:4 md:6 sm:12"`) a `preload=true` sa tak do prehliadača pošle preload hint pre KAŽDÝ breakpoint zvlášť, pričom `$size` odovzdaný do `add()` je iba `sizes` fragment PRE TENTO JEDEN breakpoint (`$size` z `formatSizePart()`), nie kompletný `sizes` reťazec cez všetky breakpointy. Výsledné `<link rel="preload" imagesizes="...">` hinty sú neúplné (obsahujú len jednu mediálnu podmienku) a navyše sa posiela viacero redundantných preload odkazov tam, kde má zmysel preloadovať iba jeden (aktuálne relevantný) variant obrázka. *(Fix: `preloadCollector->add()` sa teraz volá NAJVIAC RAZ za celé `generate()`, po skončení cyklu cez `$assignments`, so `srcset` a `sizes` zloženými zo VŠETKÝCH breakpointov (presne ako reálny `<img srcset sizes>` kombinuje kandidátov naprieč breakpointami do jedného páru atribútov) — zodpovedá odporúčanému vzoru pre preloadovanie responzívnych obrázkov (kombinovaný `imagesrcset`/`imagesizes` v jednom `<link>`). Pokryté testami `testPreloadIsAddedOnceWithCombinedSrcsetAndSizesAcrossAllBreakpoints` a `testPreloadIsNotAddedWhenDisabled`; existujúci funkčný test `ImageComponentTest::testPreloadHeaderWithSrcset` aktualizovaný na nový kombinovaný formát (`imagesizes="(min-width: 1024px) 100px, 100vw"` namiesto neúplného `100vw`).)*
- [OPRAVENÉ] **ResponsiveAttributeGenerator.php:99-105** — pri koreňovom breakpointe (`0 === $layout['min_viewport']`) sa `$variables['--img-aspect'] = (string) $ratio;` nastaví BEZPODMIENEČNE, aj keď `$ratio` je `null` (napr. chýba `$originalHeight` aj explicitný `ratio`). `(string) null` je prázdny reťazec, teda sa vygeneruje neplatná CSS premenná `--img-aspect: ;`. O tri riadky nižšie sa pritom analogická premenná pre konkrétny breakpoint (`--img-aspect{suffix}`) nastavuje len vnútri `if ($ratio)` — nekonzistentná ochrana pred prázdnou hodnotou medzi koreňovou a breakpoint-špecifickou premennou. *(Fix: root-level priradenie presunuté dovnútra `if ($ratio)`, rovnaká ochrana ako pri suffixovanej premennej. Pokryté testom `testImgAspectVariableIsNotSetWhenRatioCannotBeResolved`.)*
- [OPRAVENÉ] **ResponsiveAttributeGenerator.php:118-120** — ak žiadne z priradení (`assignments`) nevyprodukuje zdroj s `media === null` (t.j. žiadny breakpoint nemá `min_viewport === 0`), funkcia ticho vráti `defaultSource` s prázdnym `srcset`/`sizes` (`new ResponsiveSource(null, '', '')`). Component (`Image.php`) toto potom vyrenderuje ako `srcset=""` bez akéhokoľvek varovania — obrázok na najmenšej/predvolenej šírke okna nebude mať funkčný zdroj, a nikde sa nezaloguje ani nevyhodí chyba, prečo. *(Fix: pridaný voliteľný `?LoggerInterface $logger = null` konštruktorový parameter — rovnaký zavedený vzor ako `Image.php` — a `$this->logger?->warning(...)` sa zavolá práve v tejto vetve, s `$path` v kontexte. Zapojené v `ProgressiveImageExtension.php` cez `new Reference('logger', ContainerBuilder::IGNORE_ON_INVALID_REFERENCE)`. Pokryté testom `testWarnsWhenNoBreakpointProducesADefaultSource`.)*
- [OPRAVENÉ] **ResponsiveAttributeGenerator.php:161 a :186 (`calculateDimensions`)** — pre "fluid"/percentuálne layouty bez `max_container` sa šírka v pixeloch (použitá na výpočet URL pre generovanie variantu) odhaduje ako `(% / 100) * 1920` — pevne zadrôtovaná hodnota 1920px reprezentujúca predpokladanú maximálnu šírku viewportu. Nie je nikde konfigurovateľná. Na širokouhlých/4K monitoroch (2560px, 3440px, 3840px) sa tak systematicky vyžiada nižšie rozlíšenie obrázka, než aké daný breakpoint v skutočnosti zaberá na obrazovke. *(Fix: nový konštruktorový parameter `int $fluidMaxWidth = 1920` (default zachovaný pre spätnú kompatibilitu) nahrádza obe zadrôtované literály; sprístupnené cez nový config uzol `responsive_strategy.fluid_max_width` v `Configuration.php` a zapojené v `ProgressiveImageExtension.php`. Pokryté testom `testFluidMaxWidthIsConfigurableInsteadOfHardcoded1920`.)*
- [OPRAVENÉ] **ResponsiveAttributeGenerator.php:207 (`generateUrl`)** — premenná `$requestedWidth` sa priradí, no nikde ďalej v metóde sa nepoužije ani nevracia — mŕtvy kód, pravdepodobne pozostatok z predchádzajúcej verzie logiky. *(Fix: odstránené ako súčasť prepisu `generateUrl()` pri riešení nasledujúceho nálezu — metóda teraz prijíma už vyriešené `$ratio`/`$context` a `$requestedWidth` už niet.)*
- [OPRAVENÉ] **ResponsiveAttributeGenerator.php:80-90** — pre každý multiplikátor (`retina` 1x/2x/...) sa v `generateUrl()` odznova prepočítava `resolveRatio($assignment)` aj `modifierProvider->applyModifiers()` s identickým výsledkom pre daný breakpoint — zbytočne opakovaná práca (parsovanie regexu, prechod všetkých modifikátorov) namiesto jedného výpočtu pred cyklom `foreach ($multipliers as $multiplier)`. *(Fix: `resolveRatio()` a `applyModifiers()` presunuté PRED cyklus `foreach ($multipliers ...)`, vypočítané raz za breakpoint a odovzdané do `generateUrl()` ako už-vyriešené parametre `$ratio`/`$context`. Pokryté testom `testModifiersAreOnlyAppliedOncePerBreakpointNotPerRetinaMultiplier`, ktorý s `retinaMultipliers=[1,2]` overuje `modify()` volané presne RAZ, nie dvakrát.)*

## src/Variant/Application/Service

- [OPRAVENÉ] **VariantSpecFactory.php:63 (`create`)** — `array_replace_recursive($filterSetRaw, $this->imageConfigs, $context)` sa použije aj na hodnoty ako `filters.resize.size` alebo `filters.crop.start`, ktoré sú v `FilterFactory` očakávané ako dvojprvkové INDEXOVANÉ polia `[width, height]`/`[x, y]` (typicky YAML sekvencia, napr. `size: [800, 600]`). `array_replace_recursive` pri dvoch indexovaných poliach zlučuje PODĽA NUMERICKÉHO INDEXU, nie ako celok — ak filter set definuje `size: [800, 600]` a `imageConfigs`/Twig kontext ho čiastočne prepíše na `size: [400]` (jednoprvkové pole, napr. autor zamýšľal zmeniť len šírku), výsledný zlúčený `size` NIE JE `[400]`, ale `[400, 600]` (index 0 prepísaný, index 1 potichu ponechaný z base vrstvy). Ide o dobre známy, ale ľahko prehliadnuteľný problém `array_replace_recursive` s číselne indexovanými poľami — v tomto kóde môže ticho pokaziť rozmery orezania/zmenšenia obrázka bez akejkoľvek chyby alebo varovania. *(Fix: `array_replace_recursive()` nahradené novou privátnou `VariantSpecFactory::mergeLayers()` — rekurzívne zlučuje ako `array_replace_recursive`, ALE indexované (list) polia sa vždy nahradia CELÉ, nikdy nezlučujú podľa indexu (`array_is_list()`, PHP 8.1+). Dôsledok: neúplné prepísanie `size`/`start` teraz vedie k okamžitému `InvalidFilterDefinition` namiesto tichého poškodenia rozmerov — presne v duchu existujúcej filozofie triedy „fail loudly, not silently". Pokryté testami `testContextResizeSizeFullyReplacesFilterSetSizeInsteadOfMergingByIndex` (musí vyhodiť výnimku) a `testContextResizeSizeThatDoesFullyOverrideBothDimensionsIsUsedAsIs` (plné prepísanie funguje ako predtým).)*
- [OPRAVENÉ] **VariantSpecFactory.php:63 vs. FilterSetRegistry.php:33-51** — `FilterSetRegistry` validuje `filter_sets` konfiguráciu eagerly pri štarte (cez `ValidateFilterSetsPass`), no validuje iba SUROVÚ (nezlúčenú) definíciu filter setu. Skutočné filtre, ktoré sa reálne použijú, vznikajú až po zlúčení s `imageConfigs` a per-request Twig kontextom (`array_replace_recursive`, pozri predchádzajúci nález) — teda práve tá časť, ktorá sa pri boote NEVALIDUJE. Úspešná validácia pri `cache:clear` tak nezaručuje, že finálna, za behu zlúčená konfigurácia bude platná — kontrast s deklarovaným zámerom triedy „typo musí zhodiť boot, nie request za behu". *(Investigované: toto je architektonicky nevyhnutné — Twig `context` je dostupný až za behu, takže plne zlúčenú konfiguráciu principiálne NIE JE možné validovať pri boote. Reálny bezpečnostný dopad rieši predchádzajúci fix: `mergeLayers()` teraz zaisťuje, že poškodená zlúčená konfigurácia vyhodí `InvalidFilterDefinition` HLASNO pri requeste (najskorší možný moment), nie ticho. Zavádzajúci docblock triedy `FilterSetRegistry` opravený, aby presne popisoval, čo boot-time validácia garantuje (iba surový `filter_sets`) a čo nie.)*
- [OPRAVENÉ] **FilterFactory.php:74-87 (`crop`) a :112-127 (`dimensions`)** — `[$x, $y] = array_values($start)` (resp. `[$width, $height] = array_values($value)`) berie hodnoty v poradí, v akom sú v poli ULOŽENÉ, nie podľa mena kľúča. Ak je `start`/`size` v konfigurácii zapísaný ako asociatívne pole s kľúčmi v „nesprávnom" poradí (napr. YAML `start: {y: 20, x: 10}`), `array_values()` vráti `[20, 10]` a `x`/`y` (resp. `width`/`height`) sa ticho PREHODIA bez akejkoľvek chyby — validácia kontroluje iba počet prvkov a že sú číselné, nie ich sémantické poradie. *(Fix: nová privátna `pair()` metóda — ak je vstup indexovaný (list) zoznam, berie hodnoty pozične (bežný YAML `[a, b]` prípad, nezmenené správanie); ak je asociatívne pole, číta explicitne podľa mena kľúča (`x`/`y`, `width`/`height`), nikdy podľa poradia uloženia. Pokryté testami `testCropStartAsAssociativeArrayIsReadByKeyNotByStorageOrder` a `testDimensionsAsAssociativeArrayIsReadByKeyNotByStorageOrder`.)*

## src/Variant/Domain/Filter/WatermarkPosition.php

- [OPRAVENÉ] **WatermarkPosition.php:20,23** — `case Top = 'top';` a `case Bottom = 'bottom';` sú odsadené tabulátorom, zvyšok enumu medzerami — ďalší (tretí) výskyt nekonzistentného odsadenia tabulátor/medzera v repozitári (po `Configuration.php` a `ResponsiveAttributeGenerator.php`), naznačuje chýbajúci/nevynucovaný code-style formátovací nástroj pri commitovaní. *(Fix: oba riadky prevedené na 4-medzerové odsadenie. Overené `php-cs-fixer --dry-run` (0 súborov na opravu).)*

## src/Variant/Domain/Service/AspectCropCalculator.php

- [OPRAVENÉ] **AspectCropCalculator.php:36 a :39 (`calculate`)** — `$cropWidth`/`$cropHeight` sa počíta ako `(int) round(...)` z pomeru pôvodných rozmerov a cieľového pomeru strán. Pri hraničných kombináciách rozmerov, kde je nezaokrúhlená hodnota tesne pod celým číslom (napr. `X.5`), môže zaokrúhlenie zväčšiť `cropWidth`/`cropHeight` o 1px NAD skutočný rozmer originálu. `$startX = max(0, min($startX, $original->width - $cropWidth))` v takom prípade vyprodukuje zápornú hornú medzu, orezanú na `0`, a následné `CropBox::within()` (riadok 29 v `CropBox.php`) potom zistí `startX + cropWidth > bounds->width` a vyhodí `InvalidFilterDefinition` namiesto toho, aby sa crop box jednoducho orezal na hranice originálu. T.j. generovanie variantu môže pri konkrétnych (zriedkavých, ale reprodukovateľných) kombináciách pôvodných rozmerov a cieľového pomeru strán zlyhať výnimkou len kvôli zaokrúhľovaniu. *(Fix: pridaný `min($cropWidth, $original->width)`/`min($cropHeight, $original->height)` clamp HNEĎ PO zaokrúhlení, samotný zaokrúhľovací výpočet (chránený komentárom triedy "ported verbatim... do not clean up") zostal nedotknutý. Poctivá poznámka k procesu overovania: napriek rozsiahlemu pokusu o nájdenie konkrétneho reprodukovateľného vstupu (numerický sweep cez milióny kombinácií `(original, target)` vrátane zámerne "škaredých" pomerov aj cez priamy test nezhody `float`-porovnania `ow/oh > tw/th` voči presnému celočíselnému `ow*th > tw*oh`) sa NEPODARILO nájsť žiadnu kombináciu, ktorá by pretečenie reálne vyvolala pri rozumných (a dokonca aj veľmi veľkých, do ~10⁶ px) rozmeroch obrázkov — matematicky, pri presnej racionálnej aritmetike `cropWidth` nemôže prekročiť `original->width` v tejto vetve, takže by k tomu muselo dôjsť len kombináciou dvoch NEZÁVISLÝCH `float` zaokrúhľovacích chýb (porovnanie vetvy + samotný výpočet), čo si pri `double` presnosti (52-bit mantisa) vyžaduje rozmery rádovo blízke 2^53 — mimo akéhokoľvek reálneho obrazového rozmeru. Test `testCropNeverExceedsTheOriginalDimensionsAcrossAWideDimensionSweep` preto NIE JE tradičný red→green TDD prípad (prechádzal aj pred opravou, overené cez `git stash`) — je to lacná, dôkazateľne bezpečná poistka (nemení správanie pre žiadny dosiahnuteľný vstup, existujúci "pixel-parity" golden dataset ostáva bit-presne rovnaký) plus regresný test pre daný invariant do budúcna.)*

## src/Variant/Infrastructure/Flysystem/FlysystemVariantStorage.php

- [OPRAVENÉ] **FlysystemVariantStorage.php:45-52 (`write`)** — zapisuje sa najprv do dočasného `$tmp` súboru a následne `move($tmp, $full)`. Ak `move()` zlyhá (napr. cieľový adresár sa nedá vytvoriť, dočasná chyba cloud adaptéra, vypršanie S3 requestu), `$tmp` súbor zostane navždy osirotený v úložisku — nikde nie je `try/catch` s upratovacím `delete($tmp)` pri zlyhaní presunu. Pri väčšej záťaži (veľa generovaných variantov) sa takto môžu hromadiť `.tmp-*` súbory bez akéhokoľvek mechanizmu na ich odstránenie. *(Fix: nová privátna `writeAtomically()` (zdieľaná aj s `writeFailMarker()`, viď nižšie) obalí `move()` do `try/catch`, na zlyhanie vykoná best-effort `delete($tmp)` a pôvodnú výnimku vyhodí ďalej. Pokryté testom `testWriteCleansUpTheTmpFileWhenMoveFails` s mockovaným `FilesystemOperator`, ktorého `move()` vyhodí `UnableToMoveFile`.)*
- [OPRAVENÉ] **FlysystemVariantStorage.php:75-78 (`writeFailMarker`) vs. :45-52 (`write`)** — hlavný zápis variantu používa atomický vzor „zapíš do tmp, potom presuň", zatiaľ čo `writeFailMarker()` zapisuje PRIAMO na finálnu cestu (`$this->filesystem->write($this->failMarkerPath($path), ...)`) bez tmp+move. Pri súbežnom zlyhaní generovania z viacerých workerov naraz (bežný scenár — viacero requestov naraz narazí na ten istý rozbitý zdroj) hrozí prekrývajúci sa/neatomický zápis do toho istého fail-marker súboru, čo môže viesť k poškodenému obsahu a nesprávne vypočítanému `failMarkerTimestamp()` (viď `failMarkerTimestamp()` nižšie). *(Fix: `writeFailMarker()` teraz volá tú istú `writeAtomically()` helper metódu ako `write()` — rovnaký tmp+move vzor, žiadna duplicitná logika. Pokryté testom `testWriteFailMarkerIsWrittenAtomicallyViaTmpAndMove`.)*
- [OPRAVENÉ] **FlysystemVariantStorage.php:88 (`failMarkerTimestamp`)** — `(int) $this->filesystem->read($marker)` bez validácie, že prečítaný obsah je skutočne číselný reťazec. Pri poškodenom/prázdnom súbore (viď predchádzajúci nález o neatomickom zápise) sa ticho vráti `(int) '' === 0`, teda dátum 1.1.1970 — `hasFreshFailMarker()` v `GenerateVariantHandler` by takýto marker vždy vyhodnotil ako „nie čerstvý" a throttling proti opakovanému generovaniu rozbitého zdroja by prestal fungovať bez akejkoľvek chybovej hlášky. *(Fix: obsah sa validuje cez `ctype_digit()`; ak nie je číselný, `failMarkerTimestamp()` vráti `null` (rovnaké ako "žiadny marker") EXPLICITNE namiesto tichého `(int)` cast-u na 0 — funkčne rovnaký efekt ako predtým pre `hasFreshFailMarker()`, ale teraz je to zámerné a zdokumentované správanie, nie náhodný vedľajší efekt, a navyše korektne odmietne aj čiastočne poškodený obsah ako `"1600000000extra"`, ktorý by `(int)` cast tiež ticho akceptoval. Pokryté testom `testFailMarkerTimestampReturnsNullForCorruptedMarkerContentInsteadOfEpochZero`.)*
- [OPRAVENÉ] **FlysystemVariantStorage.php:70-73 (`publicPath`)** — cesta k variantu (odvodená okrem iného z pôvodnej `SourcePath`, ktorá povoľuje takmer ľubovoľné znaky okrem `..` segmentov) sa vloží do verejnej URL bez `rawurlencode()`. Zdrojový súbor s medzerou, `#`, `?` alebo iným špeciálnym URL znakom v názve (`SourcePath` také znaky nezakazuje) vyprodukuje nesprávne/nejednoznačnú verejnú URL. *(Fix: každý `/`-oddelený segment cesty sa teraz `rawurlencode()`-uje samostatne (aby sa nezakódoval samotný `/` oddeľovač na `%2F`) a znovu spojí. Pokryté testom `testPublicPathEncodesSpecialCharactersInEachSegment`.)*
- [OPRAVENÉ] **FlysystemVariantStorage.php:93-95 (`formatOf`)** — `OutputFormat::from($formatSegment)` vyhodí natívny `\ValueError`, ak prvý segment cesty nie je platná hodnota enumu (napr. cudzí/poškodený súbor v úložisku). Zvyšok triedy dôsledne vyhadzuje `VariantDomainException` (napr. `read()` na riadku 59) — tu uniká surová, nezachytená PHP výnimka namiesto domain-space výnimky, nekonzistentné s vlastným API triedy. *(Fix: `\ValueError` sa chytí a znovu vyhodí ako `VariantDomainException`. Poznámka k testovateľnosti: `VariantPath` má zámerne privátny konštruktor — jediná verejná cesta `VariantPath::for()` vždy skladá prvý segment zo skutočnej hodnoty `OutputFormat`, takže tento stav je cez reálne verejné API v skutočnosti NEDOSIAHNUTEĽNÝ (rovnaká situácia ako pri `AspectCropCalculator` vyššie). Test `testReadThrowsDomainExceptionForACorruptedFormatSegmentInsteadOfLeakingValueError` preto použil reflection na skonštruovanie `VariantPath` s neplatnou hodnotou — jediný spôsob, ako toto obranné správanie vôbec otestovať.)*

## src/Variant/Infrastructure/Intervention/InterventionImageManipulator.php

- **InterventionImageManipulator.php:110-117 (`encoderFor`)** — pre `OutputFormat::Png` sa vytvára `new PngEncoder()` úplne BEZ parametra kvality — `Intervention\Image\Encoders\PngEncoder` (overené vo vendore) ani nemá parameter `quality`, iba `interlaced`/`indexed`. Bundle pritom v `Configuration.php:220` ponúka `formats.quality.png` (default 90) ako plnohodnotnú konfiguračnú voľbu a táto hodnota sa cez `VariantSpecFactory`/`Quality` prenáša až do `VariantSpec::quality` — kde sa ale pre PNG nikdy nepoužije. Horšie: `VariantSpec::canonical()` zahŕňa `quality` do hashovaného kánonického tvaru bez ohľadu na formát (`VariantSpec.php:35`), takže zmena `formats.quality.png` medzi dvoma hodnotami vyprodukuje DVA RÔZNE `VariantId` pre bajtovo identický PNG výstup — zbytočná duplicitná generácia a uloženie toho istého obrázka pod rôznymi hashmi, a konfiguračná voľba, ktorá nemá pre PNG žiadny reálny efekt, bez akéhokoľvek upozornenia používateľovi bundle.

## src/Variant/Infrastructure/Messenger/MessengerGenerationDispatcher.php

- **MessengerGenerationDispatcher.php:30 vs. ProgressiveImageExtension.php:519-522** — trieda má vo vlastnom docblocku (riadok 22) explicitne uvedené „Per-request dedup only" a interné pole `$dispatchedThisRequest` predpokladá, že inštancia žije len jeden request. Na rozdiel od `ResolveVariantUrlHandler` (ktorý má analogickú požiadavku zdokumentovanú a v DI registrácii aj vynútenú cez `->setShared(false)`, `ProgressiveImageExtension.php:388`), registrácia `MessengerGenerationDispatcher` (`ProgressiveImageExtension.php:519-522`) `setShared(false)` NEMÁ — služba je teda predvolene zdieľaný singleton v rámci kontajnera. V klasickom PHP-FPM nasadení (nový proces/kontajner na request) to prakticky nevadí, ale pri behu na perzistentných workeroch (Swoole, RoadRunner, FrankenPHP worker mode) by `$dispatchedThisRequest` rástlo a pretrvávalo naprieč viacerými HTTP requestami, čím by sa porušil vlastný predpoklad triedy o deduplikácii „len pre tento request".

## src/Variant/Infrastructure/PostProcess

- **CliPostProcessor.php:39** — `mkdir($dir)` (bez kontroly návratovej hodnoty) a `file_put_contents()` na riadku 44 (výsledok tiež nekontrolovaný) môžu zlyhať (chýbajúce oprávnenia, plný disk, neexistujúci `sys_get_temp_dir()`). Namiesto jasnej chyby „nepodarilo sa vytvoriť dočasný adresár" by v takom prípade proces skončil až na zlyhaní externého CLI nástroja nad chýbajúcim/prázdnym vstupným súborom — matúca diagnostika.
- **CliPostProcessor.php:38-39** — dočasný adresár sa vytvára cez `mkdir($dir)` bez explicitného (reštriktívneho) módu; na zdieľanom serveri s benevolentným umaskom môže byť dočasný adresár s rozpracovaným obsahom obrázka čitateľný pre iných lokálnych používateľov/procesy.
- **CliPostProcessor.php:81-89 (`readResult`, základná implementácia) vs. JpegoptimPostProcessor.php:34-42 (override)** — základná implementácia `readResult()` (použitá `CwebpPostProcessor`, `PngquantPostProcessor` aj `AvifencPostProcessor`) kontroluje iba `false === $contents` (zlyhanie `file_get_contents`), ale NIE prázdny reťazec. `JpegoptimPostProcessor` si svoj override tejto metódy explicitne opravil na kontrolu `'' === $contents`. Ak externý nástroj (cwebp/pngquant/avifenc) skončí s úspešným exit kódom, ale vyprodukuje prázdny (0-bajtový) výstupný súbor — reálny scenár pri probléme s diskom alebo prerušenom zápise — základná implementácia to ticho akceptuje ako platný výsledok, `GeneratedImage::withContents('')` sa uloží do variant store a používateľom sa začne servovať prázdny/rozbitý obrázok bez akejkoľvek chyby v pipeline.
- **CwebpPostProcessor.php:27 a AvifencPostProcessor.php:36** — `buildCommand()` v žiadnom z týchto dvoch post-procesorov neodovzdáva binárke žiadny parameter kvality (`cwebp` nemá `-q`, `avifenc` nemá `-q`/`--min`/`--max`), hoci bundle ponúka `formats.quality.webp` a `formats.quality.avif` (`Configuration.php:218-219`). Keď je príslušný post-processor zapnutý, finálny výstup vzniká PREKÓDOVANÍM cez tento externý nástroj s jeho VLASTNÝMI predvolenými nastaveniami kvality — nakonfigurovaná hodnota kvality teda ovplyvní iba prvé (Intervention) kódovanie, ktoré sa vzápätí zahodí a nahradí výstupom s inou, nekonfigurovateľnou kvalitou. Rovnaký druh chyby ako pri `PngEncoder` v `InterventionImageManipulator.php`, tu ale v dvoch ďalších formátoch a z iného dôvodu (chýbajúci CLI parameter namiesto chýbajúcej podpory v knižnici).

## src/Variant/Infrastructure/Presentation/Controller/ImageVariantController.php

- **ImageVariantController.php:96-106** — `catch (\Throwable $e) { $this->logger?->warning(...); }` navonok vyzerá, že zlyhanie synchrónneho generovania sa aspoň zaloguje, no `$logger` je nullable parameter (`private ?LoggerInterface $logger = null`) a DI ho zapája cez `new Reference('logger', ContainerBuilder::IGNORE_ON_INVALID_REFERENCE)` (`ProgressiveImageExtension.php:405`) — teda ak konzumujúca aplikácia nemá zaregistrovanú/dostupnú službu `logger` (napr. minimálny/custom kernel bez PSR-3 loggera), `$this->logger` bude `null` a `?->warning(...)` sa jednoducho NEVYKONÁ. Výnimka sa v tom prípade prehltne bez akejkoľvek stopy — jediné, čo po nej ostane, je fail marker v úložisku a fallback na originálny obrázok, čo pri opakovaných zlyhaniach vyzerá ako "obrázky sa tíško nezobrazujú v požadovanom formáte" bez jediného riadku diagnostiky.

## src/Variant/Infrastructure/Presentation/UrlGenerator/VariantResponsiveImageUrlGenerator.php

- **VariantResponsiveImageUrlGenerator.php:83-84** — `[$poiX, $poiY] = explode('x', $pointInterest, 2);` pri vstupe bez `'x'` (napr. preklep `"100"` namiesto `"100x200"`) vráti `explode()` jednoprvkové pole; `$poiY` tak zostane `null` (s PHP warningom "Undefined array key"), `(int) null` je `0`. Namiesto jasnej validačnej chyby sa point-of-interest ticho posunie na Y=0.
- **VariantResponsiveImageUrlGenerator.php:114-119 (`negotiateFormat`)** — porovnanie `Accept` hlavičky je čisté `str_contains($accept, ...->mime())`, bez skutočného parsovania Accept syntaxe (q-hodnoty, poradie preferencií, wildcard typy). Klient, ktorý pošle `Accept: image/*` alebo `*/*` (akceptuje ľubovoľný obrázkový formát) bez explicitného výpisu `image/webp`/`image/avif`, sa touto substring-kontrolou vôbec nezachytí — negociácia formátu preňho vždy zlyhá a dostane predvolený formát, hoci by mal dostať optimalizovaný.
- **VariantResponsiveImageUrlGenerator.php:86** — pri zadanom `pointInterest` sa pôvodné rozmery obrázka získavajú cez `MetadataReaderInterface::getMetadata()` — teda cez CELÚ starú (pred-DDD) analyzer pipeline (`src/Service/MetadataReader.php` → `GdImageAnalyzer`/`ImagickAnalyzer`), ktorá pri cache-miss vždy počíta aj blurhash (drahá CPU operácia), hoci tu sa z výsledku využije iba `width`/`height`. Každý POI-orezaný obrázok pri prvom (necachovanom) requeste tak zbytočne spustí plný výpočet blurhash, ktorého výsledok sa v tomto kontexte vôbec nepoužije.

## src/Variant/Infrastructure/Symfony/DefaultOriginalUrlResolver.php

- **DefaultOriginalUrlResolver.php:28** — `'/'.$source->value` bez `rawurlencode()` — rovnaký problém ako pri `FlysystemVariantStorage::publicPath()` (viď vyššie): `SourcePath` môže obsahovať medzery a iné URL-špeciálne znaky, ktoré sa tu vložia do vrátenej URL nezakódované.

## src/Variant/Infrastructure/Sync/SyncGenerationDispatcher.php (kritický nález)

- **SyncGenerationDispatcher.php:30-33 (`dispatch`)** — volá `($this->handler)($command)` (teda `GenerateVariantHandler::__invoke()`) BEZ `try/catch`. `GenerateVariantHandler` pri zlyhaní generovania (poškodený zdroj, chýbajúci watermark obrázok, zlyhaný CLI post-processor, plný disk...) explicitne `throw $e;` znovu vyhodí výnimku PO zapísaní fail markera (`GenerateVariantHandler.php:83-88`). Keď je nakonfigurované `generation.strategy: sync`, táto výnimka sa odtiaľto nezachytená prepadne až do `ResolveVariantUrlHandler::resolve()` (ktorý volá dispatcher priamo v request flow, `ResolveVariantUrlHandler.php:83`) a odtiaľ rovno do Twig renderovania stránky — CELÝ request skončí chybou 500 namiesto toho, aby zabral práve navrhnutý fallback mechanizmus (`PendingFallbackStrategy::Original`/`Wait`), ktorého explicitným účelom je pri zlyhaní/čakaní na generovanie gracefully spadnúť na pôvodný obrázok. Pri `async`/`terminate` stratégii k tomuto nedochádza, lebo generovanie beží mimo aktuálneho requestu (worker / kernel.terminate) — `sync` stratégia je tak jediná, kde chyba pri generovaní obrázka zhodí celú stránku namiesto zobrazenia pôvodného obrázka.

## src/Variant/Infrastructure/Terminate/TerminateGenerationDispatcher.php

- **TerminateGenerationDispatcher.php:23-24 vs. ProgressiveImageExtension.php:506-513** — docblock triedy explicitne hovorí „this class must be request-scoped (non-shared), same requirement as ResolveVariantUrlHandler". DI registrácia v `configureVariantDispatcher()` (vetva `'terminate' === $strategy`) však `->setShared(false)` NEVOLÁ (na rozdiel od `ResolveVariantUrlHandler`, ktorá ho má na riadku 388) — presne ten istý typ nezrovnalosti medzi zdokumentovaným predpokladom a skutočnou DI registráciou ako pri `MessengerGenerationDispatcher` (viď vyššie). Trieda je navyše registrovaná aj ako `kernel.event_listener` (jeden listener zaregistrovaný raz pri buildovaní kontajnera) — v klasickom PHP-FPM nasadení to funguje len preto, že kontajner sa buduje nanovo na request; pri perzistentných workeroch (Swoole/RoadRunner/FrankenPHP) by interné pole `$queued` mohlo zdieľať/miešať naframované príkazy medzi rôznymi (aj súbežnými) requestami.
- **TerminateGenerationDispatcher.php:40-48 (`onTerminate`)** — `foreach ($queued as $command) { ($this->handler)($command); }` bez `try/catch` okolo volania handlera. Ak generovanie PRVÉHO frontu zlyhá výnimkou, `foreach` sa preruší a všetky NASLEDUJÚCE naframované príkazy v tom istom `queued` fronte (obrázky z tej istej stránky, ktoré ešte čakali na spracovanie po `kernel.terminate`) sa vôbec nespracujú — potichu prepadnú bez pokusu o generovanie, kým ich nevyžiada ďalší request.

